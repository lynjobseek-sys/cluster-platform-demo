apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  namespace: frontend-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
        - name: web
          image: ghcr.io/example/web:1.4.2
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
